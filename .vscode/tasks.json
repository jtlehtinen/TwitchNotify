{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build-debug",
      "command": "build.cmd",
      "type": "process",
      "args": ["debug"],
      "options": {
        "cwd": "${workspaceFolder}"
      },
      "group": {
        "kind": "build",
        "isDefault": true
      }
    }
  ]
}
